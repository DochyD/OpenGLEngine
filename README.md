# OpenGLEngine

Dev process:

```
cd /d/dev/repos/OpenGLEngine
cmake -B build -G Ninja
cmake --build build
./build/OpenGLEngine.exe
```

cmake -B build -G Ninja is only when we update cmake/add source file..

# TODO : Next step
# Understand code in main.cpp

# ruff/ty for cpp